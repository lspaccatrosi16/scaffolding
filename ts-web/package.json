{
	"name": "example-website",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev:vite": "vite",
		"dev": "nodemon -e html,css,ts,md -x npm run preview",
		"build": "node prebuildtool/dist/pbt.cjs && tsc && vite build",
		"preview": "npm run build && npx live-server --port=4173 --no-browser dist"
	},
	"devDependencies": {
		"nodemon": "^2.0.22",
		"typescript": "^4.9.3",
		"vite": "^4.2.0"
	},
	"dependencies": {
		"dayjs": "^1.11.7"
	}
}