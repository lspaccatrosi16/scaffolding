{
	"name": "example-website",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev:vite": "vite",
		"dev": "nodemon -e html,css,ts,md -x npm run preview",
		"build": "tsc && vite build",
		"preview": "npm run build && npx live-server --port=4173 --no-browser dist"
	},
	"devDependencies": {
		"nodemon": "^3.1.4",
		"typescript": "^4.9.3",
		"vite": "^4.2.0"
	}
}
